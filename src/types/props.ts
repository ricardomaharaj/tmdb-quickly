export type MovieProps = {
  id: string
  query: string
  page: number
}

export type TVProps = {
  id: string
  query: string
  page: number
}

export type SeasonProps = {
  id: string
  season_number: number
  query: string
  page: number
}
